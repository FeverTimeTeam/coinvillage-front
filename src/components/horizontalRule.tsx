import styled from 'styled-components';
import color from '../constants/color';

const StyledHorizontalRule = styled.hr`
  border: solid 0.063rem ${color.warm_gray_deep};
`;

export default StyledHorizontalRule;
